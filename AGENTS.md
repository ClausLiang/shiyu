# 拾语 · 项目协作约定

本文件适用于整个仓库，供参与开发的编码助手使用。用户在当前对话中的最新明确要求优先；需求变化后同步更新本文件与相关文档。

## 名称与范围

- 中文名称统一为「拾语」，英文标识使用 `shiyu`，包名为 `shiyu-english`。
- 产品包含单词、语句、文章三个学习方向。当前阶段完善英语四级单词学习，语句、文章、账号与远端同步属于后续计划。
- 已确认的产品细节以 `docs/PLAN.md` 为准，完成情况与验证结果记录在 `docs/PROGRESS.md`。
- 不把规划中的功能描述为已实现，也不提前引入后端、账号体系或远端服务。

## 开发原则

- 项目处于初期，优先清晰、简单、可维护的实现，不为假设中的未来需求增加抽象或依赖。
- 可以直接改进不完善的旧设计；不为保留早期测试数据添加旧接口适配、存储键回退或迁移逻辑，除非用户另有要求。
- 修改存储结构或键名会使原进度不再读取时，在交付说明和进度文档中写清影响；无需仅为保留初期测试数据阻塞改进。
- 常规实现选择自主推进。涉及学习机制、内容范围或技术架构等方向变化时，及时与用户讨论。
- 与用户使用中文交流；保留已有代码风格，只修改本次任务需要的内容。

## 技术与文件分工

当前使用原生 HTML、CSS、JavaScript ES Modules 和 Node.js 22+，无需第三方运行依赖。

- `index.html`：页面入口与浏览器标题。
- `src/main.js`：界面渲染与用户交互。
- `src/style.css`：桌面和移动端样式。
- `src/learning.js`：章节、拼写判定、进度统计与数据校验等纯逻辑。
- `src/storage.js`：本地存储读写及存储键常量。
- `public/data/`：词库、来源说明与许可证文本。
- `scripts/`：本地服务与静态构建。
- `tests/`：核心逻辑测试及测试样例。
- `dist/`：生成的构建产物；修改源文件后重新构建，不直接编辑产物。

## 学习与数据约定

- CET-4 词库为 2607 个唯一词条，按原顺序每 20 词一章，共 131 章，最后一章 7 词。
- 点击卡片后单词模糊，输入提交后检查；正确恢复单词，错误允许重试，查看答案不计完成。
- 拼写忽略大小写和首尾空白，其余按词库匹配。一次拼对表示「已练习」，不等于长期掌握；重复练习不重复累计已练习词数。
- 当前章节和已提交练习记录自动写入 localStorage，刷新后恢复；正在输入但尚未提交的内容不属于已保存进度。
- 当前存储键为 `shiyu:learning:v1`，通过 `src/storage.js` 的 `STORAGE_KEY` 统一引用，避免各处复制字面量。
- 存储失败应给出明确提示，不能显示为保存成功；导入文件先校验，再修改进度。
- 词库随项目保存，保留来源及许可记录。更换词库或调整词序时说明对章节与进度的影响，不自行编造词条内容。

## 开发与验证

- `npm run dev`：启动本地服务，默认地址为 `http://127.0.0.1:5173`。
- `npm test`：执行核心逻辑测试。
- `npm run build`：生成静态产物 `dist/`。
- `npm run preview`：预览构建产物，默认与开发服务使用同一端口，不同时启动。
- 固定使用相同浏览器、主机名和端口检查进度；不同地址的 localStorage 相互独立。服务没有热更新，改动后需刷新。
- 按改动范围验证：学习或存储逻辑改动运行相关测试；页面、脚本或资源改动执行构建；交互或布局改动在浏览器检查受影响流程，必要时检查手机宽度。
- 只改文档时检查内容、路径和链接，无需启动服务或运行应用测试。不要为简单文案或纯样式调整编写只复述实现的测试。
- 尊重用户对服务启停的要求；验证结束后说明服务状态。停止服务时只停止本项目启动的进程。

## 文档维护与交付

- `README.md` 说明项目用途、启动和使用方式；`docs/PLAN.md` 记录产品计划；`docs/PROGRESS.md` 持续记录实现进度；本文件维护稳定协作约定。
- 每次完成实质改动后更新 `docs/PROGRESS.md`，记录改了什么、实际验证了什么以及仍未完成的内容。
- 只有相关约定发生变化时才更新本文件；不要把临时调试过程和完整对话写进来。
- 交付时简要说明结果、验证情况和对使用有影响的变化。未经执行的测试不能标为通过。
