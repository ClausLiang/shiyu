# 拾语 · 英语学习小工具

一个先从英语四级单词开始的网页版学习工具。第一版包括 2607 个单词、131 个章节、卡片拼写和浏览器本地进度；语句、文章、账号和云端同步留待后续实现。

在线使用：[拾语](https://liangyonggang.com/shiyu/)。已通过 GitHub Pages 发布，默认地址 https://clausliang.github.io/shiyu/ 会重定向到该地址。

## 启动

需要 Node.js 22 或更高版本，无需安装第三方依赖。

```sh
npm run dev
```

打开 http://127.0.0.1:5173 。本地服务只监听本机地址。

```sh
npm test          # 核心逻辑测试
npm run build    # 生成静态文件到 dist/
npm run preview  # 在同一端口预览 dist/，先关闭 dev 服务
```

服务没有热更新；修改代码后刷新浏览器。`PORT` 环境变量可指定端口。构建产物支持部署到静态托管根路径或子目录。

## 如何学习

1. 选择章节，一页显示 20 个单词，最后一章显示 7 个。页面右上角常显当前章节与全书进度，点击打开章节目录（手机端悬浮在右下角）。
2. 点击卡片，单词变模糊，保留中文提示，并自动发音一次；通过“开始 / 继续本章练习”进入拼写时也会自动发音。输入后按 Enter 或箭头按钮检查。
3. 拼对后显示单词、播放一声提示音并记为已练习；拼错时可继续尝试。按 Esc，或点击模糊的单词 / 中文释义，即可查看答案，不计完成。
4. 在拼写状态下按 Tab 会先检查拼写，只有拼对才切换到本章下一个单词的拼写状态（等同于按 Enter 检查）；查看状态下按 Tab 则直接进入下一个单词的拼写。
5. 拼对本章最后一个单词后按 Tab 会弹出庆祝窗并播放一段专属的完成旋律，可以进入下一章（最后一章则回到第一章）、再巩固一遍或留在这里。首次学习时需要本章所有单词都已完成才会庆祝，中途漏词则不弹；复习已经学完的章节时，走完整轮（拼对末词）即可再次获得这份奖励。
6. 从侧栏“进度与备份”或页面右上角保存状态进入备份窗口。
7. 点击每张卡片右侧的发音栏播放发音，拼写中也能听，不会提交或清空输入。连续点击会切换播放，切章或隐藏 / 离开页面时停止。

发音采用浏览器内置语音，优先美式英语（优先本地音色），没有美式时使用其他英语语音。音色随浏览器和设备变化，部分语音需要联网；若提示缺少英语语音，请在设备语音设置中安装英语语音后重试。不需要后端、API 密钥或麦克风权限。

Windows Chrome 若无声：先完全退出并重新打开浏览器。若仍失败，请查看页面提示中的具体音色及“本机语音 / 在线语音”；在线语音可能因网络无法响应。本机英语语音可在“设置 → 时间和语言 → 语音 → 添加语音”中添加英语（美国），安装后重启浏览器。仅安装英语界面或键盘不等于已安装语音。

一次拼对只代表已练习，不代表已经长期掌握。拼写不区分大小写、忽略首尾空白；保留词库中的英美拼写原样，例如 `analyse` 需按卡片上的形式拼写。

## 本地记录

- 使用版本化 localStorage 数据，保存当前章节、每词尝试次数、正确次数、完成状态和最近练习时间。
- 进度按浏览器及网站地址隔离；请固定使用同一个地址。`localhost` 与 `127.0.0.1`、不同端口、不同浏览器的记录不互通。
- 清除浏览器数据会清除进度。换设备前可导出 JSON 备份，在新设备导入。
- 导入前校验结构、版本、词库、词条与计数。与当前进度合并，保留当前章节；完成状态取并集，次数取较大值，重复导入不会重复计数。
- 备份不是跨设备实时同步；离线分支的尝试次数不会相加。后续账号版本需引入独立练习事件 ID。
- 本地数据损坏时不会自动覆盖原始数据；当前页进入临时学习模式，提示导出备份。存储失败时不会假装保存成功。
- 首次读取词库需通过本地 HTTP 服务，不能直接双击 HTML 文件。没有 Service Worker，不承诺关闭服务后仍可重新打开。

## 项目文档

- [编码助手协作约定](AGENTS.md)

- [GitHub Pages 部署说明](docs/DEPLOYMENT.md)
- [项目计划](docs/PLAN.md)
- [实现进度与验证记录](docs/PROGRESS.md)
- [词库来源及许可说明](public/data/SOURCE.md)

## 目录

```text
src/main.js       页面及交互
src/style.css     响应式界面
src/learning.js   章节、拼写、统计、校验与合并逻辑
src/storage.js    浏览器存储适配层
src/pronunciation.js 浏览器发音与异常处理
src/feedback.js   拼写正确与学完整章的奖赏音效合成
public/data/      原始词库及来源说明
scripts/          本地服务与静态构建
tests/            核心逻辑测试与导入样例
```

## 后续讨论

优先根据实际体验调整卡片和章节流程；再讨论复习机制、发音体验改进，以及语句和文章学习。远端免费服务需在实施时核对区域访问、免费额度、休眠政策和数据迁移方式。
